# TODO

- [ ] Google Auth — authentication for cross-device sync
- [x] Mood/Journal correlation on dashboard — shipped in ship/mood-insights-dashboard
- [x] Fix pre-existing tsc errors (was 403 errors across all test files, not just sleep) — shipped in ship/fix-tsc-errors-and-mood-hint
- [x] Add mood trend to dashboard empty state hint — shipped in ship/fix-tsc-errors-and-mood-hint
- [ ] Fix git push permissions — keegan-stepper SSH key lacks write access to kjb135-coe/trackr.git
- [ ] Add tsc --noEmit to CI/pre-commit hook now that it passes cleanly
